import 'package:flutter/material.dart';

class AppConfig {
  static const appName = 'IsySHOP';
  static const defaultLocale = Locale('fr');
  static const supportedLocales = [Locale('fr'), Locale('en')];
}
